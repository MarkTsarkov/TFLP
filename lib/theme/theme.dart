import 'package:flutter/material.dart';

final theme = ThemeData(
        primarySwatch: Colors.blue,
        primaryColor: const Color.fromARGB(100, 14, 105, 240),

        appBarTheme: const AppBarTheme(
          color: Colors.white, 
          iconTheme: IconThemeData(color: Color.fromARGB(100, 14, 105, 240))
          )
);